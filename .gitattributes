# explore/gallery.html is ~80% machine-generated by scripts/build.js (the
# masonry tile markup between the BUILD markers). Mark it linguist-generated so
# GitHub collapses its diff in pull requests and blame, and excludes it from the
# repo's language stats. The bytes are still committed (GitHub Pages serves them
# directly, branch-deploy), this only keeps the history readable.
explore/gallery.html linguist-generated=true
