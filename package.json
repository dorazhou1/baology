{
  "name": "baology",
  "version": "0.0.0",
  "private": true,
  "description": "Baology Prep static site",
  "scripts": {
    "build": "node scripts/build.js",
    "prepare": "git config core.hooksPath .githooks || true"
  },
  "devDependencies": {
    "image-size": "^2.0.2",
    "js-yaml": "^4.1.0"
  }
}
